[済]
- ラムダ式から外の変数のメンバを参照できない
- baseに存在しない関数を呼ぶと落ちる
- インスタンスメンバをクラスメンバ扱いすると落ちる
- 接頭辞non-フォルダをビルドから除外
- クラスメンバの二重null初期化を回避

[バグ]
- 多重base
- メンバのエラー位置
- クラスの自動変数定義を禁止
- 静的メンバに対するエラーメッセージが読みにくい
- 符号が絡むキャスト: ex. (int)(ushort)(short)65535
- メンバから関数ポインタが呼べない

[予定]
- 内部クラスのインスタンス属性（ドメイン）

[気になる動作]
- プロパティの設定と取得が同時に行えない
- ポインタ演算の引き算
- callのretが64bitのときに怪しい
-- 64bit自体未サポートだが
- エラーテストの枠組みがない

[自己記述後]
- switchのテーブル化
- インタプリタとASTを共用
- アクセス修飾子
- 演算子オーバーロード
- リフレクション
- 委譲を参照型にする
-- レキシカルスコープを共有型にする
- foreach
- インライン関数
- 名前空間
- 64ビット整数型 (long, ulong)
- 浮動小数点数型 (float, double)
- 内部オブジェクトの再利用

[アイデア]
- 部分継承
-- アスペクト指向
- モンテカルロ探索
-- 品種改良
-- 遺伝的アルゴリズム
